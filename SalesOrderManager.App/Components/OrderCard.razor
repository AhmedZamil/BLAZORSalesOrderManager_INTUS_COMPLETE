


<div class="col">
    <div class="card rounded-3 employee-card">
        <div class="card-header py-3">
            <h4>@Order.Name @Order.State.Name</h4>
        </div>
        <div class="card-body employee-card text-center">
           
            <button class="btn btn-primary btn-sm mb-1" @onclick="@(async () => await OrderQuickViewClicked.InvokeAsync(Order))"> Quick view</button>
            <a href="@($"orderdetail/{Order.OrderId}")" class="btn btn-outline-primary btn-sm mb-1">
                Full details
            </a>
@*            <button class="btn btn-outline-primary btn-sm mb-1" @onclick="(() => NavigateToDetails(Employee))">
                Full details
            </button>*@

             <a href="@($"orderedit/{Order.OrderId}")" class="btn btn-outline-primary btn-sm mb-1">
                Edit
            </a>
            <button class="btn btn-primary btn-sm mb-1"@onclick="@(async () => await OrderRemoveClicked.InvokeAsync(Order))"> Remove</button>
        </div>
    </div>
</div>


